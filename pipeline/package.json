{
  "name": "cosmediloans-seo-pipeline",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "pipeline": "tsx src/run.ts --mode=scheduler",
    "collect": "tsx src/run.ts --mode=collect",
    "analyze": "tsx src/run.ts --mode=analyze",
    "alerts": "tsx src/run.ts --mode=alerts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "googleapis": "^148.0.0",
    "node-cron": "^3.0.3",
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0",
    "@types/node": "^22.0.0",
    "@types/node-cron": "^3.0.11"
  }
}
